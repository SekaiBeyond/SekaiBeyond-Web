# Origin used to build ticket-claim URLs embedded in QR codes
# (sendTicketEmails -> {PUBLIC_ORIGIN}/claim?ticket=X&event=Y).
# Defaults to https://sekaibeyond.com in code. Forks MUST override this or
# their tickets will point at the original deployment. An explicit empty
# value (PUBLIC_ORIGIN=) still trips sendTicketEmails' failed-precondition
# guard, so leaving this unset is safer than blanking it.
PUBLIC_ORIGIN=https://your-site.example.com

# Verified sender on the Resend account. All outbound mail uses this as the
# From header — Resend rejects anything not on a verified domain.
# Defaults to mika@sekaibeyond.com in code.
# RESEND_FROM_ADDRESS=mika@sekaibeyond.com

# NOTE: the Resend API key is NOT set here. It is a secret managed by
# Google Secret Manager (defineSecret in resendClient.ts). Set it with:
#   firebase functions:secrets:set RESEND_API_KEY

# Optional. Per-day cap on outgoing emails (matches the Resend plan tier).
# Enforced server-side against the cached x-resend-daily-quota header.
# Default: 100 (Resend free plan).
# RESEND_DAILY_CAP=100

# Optional. Max attendees processed per sendTicketEmails invocation, also the
# per-call Resend batch size (Resend caps batches at 100). The client loops
# with cursor/drain pagination. Default: 100.
# SEND_CHUNK_SIZE=100

# Optional. Max attendee rows per importEventAttendees call. Default: 1000.
# IMPORT_MAX_ROWS=1000

# Optional. Max payload size for bulk uploads in megabytes. Default: 10.
# MAX_UPLOAD_SIZE_MB=10
